apiVersion: v1
kind: Service
metadata:
  name: concours
  labels:
    app: concours
  annotations:
    beta.cloud.google.com/backend-config: '{"default": "config-default"}'
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: concours
